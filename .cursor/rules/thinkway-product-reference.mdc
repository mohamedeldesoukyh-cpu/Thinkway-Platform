---
description: Thinkway product reference — hierarchy, terminology, workflows, and implementation guardrails
alwaysApply: true
---

# Thinkway Product Reference (Source of Truth)

Before designing schema, UI, workflows, or new modules, read:

- `docs/architecture/THINKWAY_ENTERPRISE_PLATFORM_ARCHITECTURE_V1.md` — **frozen platform architecture v1.0**
- `docs/architecture/BUSINESS_PROCESS_NAVIGATION_FOUNDATION.md` — **canonical Business Process Navigation**
- `docs/architecture/PLATFORM_ARCHITECTURE_COMPLIANCE.md` — mandatory compliance (lifecycle · journeys · BPN reuse · no new nav)
- `docs/architecture/platform-ux/12-CAMPAIGN_LIFECYCLE_ARCHITECTURE.md` — campaign lifecycle SSOT
- `docs/capabilities/PLANNING_BOARD_CAPABILITY_SPEC.md` — next functional gate (approve before 2.2a coding)
- `docs/THINKWAY_SYSTEM_REFERENCE.md` — canonical product spec (May 2026)
- `docs/ARCHITECTURE_ALIGNMENT.md` — current codebase vs spec gap analysis
- `docs/architecture/CAMPAIGN_MODULE_BASELINE.md` — **protected Campaign module baseline** (closed 2026-08-01)
- `docs/architecture/PRODUCT_UX_STANDARDS.md` — platform UX standards (Campaign = canonical)
- `docs/architecture/CAMPAIGN_INFORMATION_ARCHITECTURE.md` — Campaign List ↔ Workspace IA
- `docs/architecture/CAMPAIGN_WORKSPACE_UI_GUIDELINES.md` — Aurora extension rules (no redesign without approval)
- `docs/architecture/FINANCIAL_DISPLAY_STANDARD.md` — ISO money presentation

**Platform principle:** Thinkway is a campaign-centric enterprise OS. Features extend the frozen lifecycle and stakeholder journeys — they do not redefine navigation, workspace, or process philosophy.

## Non-negotiable hierarchy (preserve)

```
Group → Legal Entity (clients) → Brand → Campaign Header → Campaign Line
```

- **Brand-first** campaign creation: selecting a brand auto-fills group, legal entity, category, VR%, currency, direct/agency.
- **Campaign numbering**: header stored `TW-YYYY-NNNN`; **UI display** `Camp#YYYY-N` via `formatDocumentNumberForDisplay`; lines `{header}-A`, `-B`, `-C` (sequential, never manual, resets yearly).
- Commercial fields (category, subcategory, VR%, direct/agency, currency) live on **brands**, not duplicated on clients or campaign headers (headers sync from brand via DB trigger).

## Terminology (UI labels)

| Reference doc | DB / code | UI label |
|---------------|-----------|----------|
| Client (legal entity) | `clients` | Legal entity |
| Brand | `brands` | Brand |
| Vendor | `influencers` (+ agencies) | Vendor / Influencer |
| Level 1 campaign | `campaign_headers` | Campaign |
| Level 2 line | `campaign_lines` | Campaign line |
| Influencer assignment | `campaign_influencers` | Vendor assignment |

## Implementation rules

1. **Compare before building** — check `ARCHITECTURE_ALIGNMENT.md` for gaps; do not duplicate entities.
2. **Operational workspaces, not CRUD** — profiles use tabs, KPI strips, sheets, filters, audit (see `/groups/[id]`, `/campaigns/[id]`).
3. **Finance on lines** — revenue, cost, GP, margin, PO, remaining PO, billing states belong at line level; aggregate at header/group.
4. **RLS + roles** — enforce Admin/Director/Manager/Account Manager/Finance/Data Entry matrix from reference §6.
5. **Approvals** — use `approvals` / `approval_steps`; extend with workflow engine tables only when needed (§20).
6. **Phased roadmap** — Phase 1: core ops; Phase 2: workflow/tasks/notifications/portals; Phase 3: AI; Phase 4: enterprise scale (reference §33).
7. **Campaign Module Baseline (protected)** — IA + Workspace UI + Enterprise Tabs + Financial Display + Deliverables selection + persistent shell are frozen. Extend only; no Campaign redesign unless Critical usability + Product approval (`CAMPAIGN_MODULE_BASELINE.md`).
8. **Product UX Standards** — Campaign is the canonical implementation for workspace architecture, navigation/information hierarchy, KPIs, summaries, Enterprise Tabs, financial presentation, status badges, action placement, and progressive disclosure (`PRODUCT_UX_STANDARDS.md`).
9. **Enterprise Tabs** — only approved workspace tab rail (`components/workspace/enterprise-tabs.tsx`). No page-specific tab implementations.
10. **Financial Display Standard** — ISO currency codes only (`formatMoneyKpi` / `formatMoneyDetail` in `lib/finance/currency-format.ts`). Never display `$`/`E£`/locale glyphs.
11. **Functional delivery next** — Planning Board (2.2a) → Media Plan Copilot (2.2b) → Vendor IO Enterprise Completion → Reporting Hub → Notifications → Enterprise Analytics.
12. **Platform UX architecture (paused)** — `docs/architecture/platform-ux/` · Campaign Lifecycle SSOT is doc **12**. **Do not implement** until Product approves doc 12 and freezes the architecture set; then Phase 1 only.


## Conflicts with reference doc (resolved in codebase)

- Reference §3.2 lists category/VR%/direct-agency on **client**; codebase correctly places them on **brand** for multi-brand legal entities. Auto-fill campaigns from brand, not client.
- Reference Level 2 = one influencer per line code; codebase uses `campaign_lines` (financial/PO) + `campaign_influencers` (vendor linkage). Treat lines as operational PO units; map influencer economics via assignments or consolidate when a line equals one creator.

## Branding

- Product: **Thinkway** · Prefix: **TW** · Primary: `#1D9E75`
