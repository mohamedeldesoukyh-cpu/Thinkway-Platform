"use client";

/**
 * Backward-compatible re-export — prefer `@/components/shared/kpi/kpi-strip`.
 */
export {
  KpiCarousel,
  KpiStrip,
  KpiMixedCurrencyNotice,
  type KpiCarouselItem,
  type KpiStripProps,
} from "@/components/shared/kpi/kpi-strip";

export type { KpiCardProps } from "@/components/shared/kpi/kpi-card";
